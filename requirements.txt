# Requires Python 3.11 (specify separately in README / CI config, not pip-installable here)

# Core
numpy==1.26.4
pandas==2.2.2
scipy==1.13.0
pyarrow==16.1.0          # parquet read/write support for pandas

# Astronomy / light curve tools
astropy==6.1.0           # BoxLeastSquares, time/units handling
lightkurve==2.4.2        # flatten(), light curve utilities
wotan==1.10               # detrending (biweight/cosine filters)
transitleastsquares==1.0.31   # TLS period search, tuned for shallow transits

# Modeling
scikit-learn==1.5.0
xgboost==2.0.3
# lightgbm==4.3.0        # uncomment if used instead of / alongside xgboost

# Plotting / notebooks (dev + write-up figures)
matplotlib==3.9.0
jupyter==1.0.0

# Config / utils
pyyaml==6.0.1
tqdm==4.66.4

# -----------------------------------------------------------------------------
# After finalizing your environment, regenerate this file with exact versions:
#   pip freeze > requirements.txt
# and re-add this header comment. Judges will run `pip install -r requirements.txt`
# in a fresh environment — untested version pins are a common reproducibility
# failure, so verify this file installs cleanly before submitting.
# -----------------------------------------------------------------------------
